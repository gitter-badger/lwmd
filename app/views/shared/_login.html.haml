.ui.attached.message
  .ui.basic.segment
    = image_tag("PTCLogo-dark.png", alt:"PTC Logo")
    %b PTC Membership Database
    %p Your resource for managing membership, accessing sponsor discounts, and more!
= form_for resource, as: resource_name, url: session_path(resource_name), :html => { class: "ui attached fluid form segment" } do |f|
  = render partial: "shared/flash_messages"
  %h2.ui.header Log in
  .field
    = render partial: "shared/omniauth_links"
  .ui.divider
  .field
    %h4.ui.header Or log in with email
  .ui.small.form.segment
    .field
      = f.label :email
      = f.email_field :email, autofocus: true, placeholder: "you@example.com", type: "text"
    .field
      = f.label :password
      = f.password_field :password, autocomplete: "off", type: "password"
    - if devise_mapping.rememberable?
      .inline.field
        .ui.checkbox
          = f.check_box :remember_me
          = f.label :remember_me
    = f.submit "Log in", class: "ui blue submit button"
  = render "devise/shared/links"
.ui.bottom.attached.info.message
  .header Not a member? Join now!
  %p
    = link_to "Become a PTC Member Today!", "http://www.active.com/event_detail.cfm?event_id=2120055", :target => "_blank"
